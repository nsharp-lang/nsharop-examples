import "Standard.Float";
import "Standard.Utf8String";
import "Standard.IO.Console";

public static class Main {

    public static async Task<int> Main(string args[]) {
        var line = await Console.ReadLineAsync();
        var float = Float.Parse(line);
        await Console.WriteLineAsync(float);
        return 0;
    }

}
